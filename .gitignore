# Node.js dependencies
node_modules/

# Electron build output
dist/
release/ # Sometimes used by electron-builder for release builds
build/*.map # Source maps are large and not always needed in repo

# Local Electron settings/caches
.electron-builder/
.eslintcache
.npm/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*